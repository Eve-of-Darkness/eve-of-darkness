language: elixir
elixir:
  - '1.7.3'
otp_release:
  - '21.0'
env:
  - MIX_ENV=test
install:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
  - mix compile
  - mix do ecto.create, ecto.migrate
script:
  - mix test
  - mix credo -a
  - mix format --check-formatted
